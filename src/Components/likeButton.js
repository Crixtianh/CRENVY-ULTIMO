/*import React, {useState} from 'react'



let estado="likeddddddddd";

function like(){
    estado="LIKEADOOOOOOO"
}

const likeButton = props => {
    //console.log(props);
    const {content} = props; //Le pasamos los valores desde entryData
    
    return (
        <div>
            <button type='button' onClick={like}>{dataEntrante} y {estado}</button>
        </div>
    );
}
export default likeButton*/